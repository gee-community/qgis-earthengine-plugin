ratelim
# Pin the Earth Engine API so vendored extlibs and runtime installs stay aligned.
# earthengine-api 1.7.x requires Python >=3.10. Older QGIS-bundled Python
# environments should stay on the newest 1.6.x release.
earthengine-api==1.6.15; python_version < "3.10"
earthengine-api==1.7.24; python_version >= "3.10"
six>=1.13
httplib2
requests>2.4
